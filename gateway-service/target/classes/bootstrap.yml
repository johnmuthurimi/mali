spring:
  application:
    name: gateway-service
  cloud:
    consul:
      host: 192.168.100.29
      config:
        format: YAML
      discovery:
        register-health-check: true

    loadbalancer:
      ribbon:
        enabled: false

server.port: 5000