server.port: ${PORT:5001}
spring:
  application:
    name: user-service
  cloud:
    consul:
      enabled: true
      port: 8500
      host: 10.0.2.15
      config:
        format: YAML
      discovery:
        instance-id: "${spring.application.name}-${random.int[1,999999]}"
        health-check-critical-timeout: 1m
        register-health-check: true
        prefer-ip-address: true
  output.ansi.enabled: ALWAYS
  rabbitmq:
    port: 5672
    host: 10.0.2.15
    username: guest
    password: guest
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3308/user_service?useSSL=false
    username: root
    password: "5Ej%0gOmr3GJybaG8@t10fIh,YD"
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: validate
logging:
  level:
    org.springframework.web.filter.CommonsRequestLoggingFilter: DEBUG

